# Example environment configuration for Binance credentials

# Copy this file to `.env` and replace the placeholders with your
# Binance API credentials. This file is used by python-dotenv when
# importing environment variables. The GUI login dialog will override
# these values if you provide credentials manually.

BINANCE_API_KEY=your_api_key_here
BINANCE_API_SECRET=your_api_secret_here
BINANCE_TESTNET=true
